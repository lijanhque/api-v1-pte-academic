// This file is intentionally left empty as we're not using middleware
// Authentication is handled via layout components and client-side checks
export type ActionState = {
  error?: string;
  email?: string;
};