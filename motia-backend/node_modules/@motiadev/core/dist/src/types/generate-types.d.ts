import type { Printer } from '../printer';
import type { Step } from '../types';
import type { Stream } from '../types-stream';
type HandlersMap = Record<string, {
    type: string;
    generics: string[];
}>;
type StreamsMap = Record<string, string>;
export declare const generateTypesString: (handlers: HandlersMap, streams: StreamsMap) => string;
export declare const generateTypesFromSteps: (steps: Step[], printer: Printer) => HandlersMap;
export declare const generateTypesFromStreams: (streams: Record<string, Stream>) => StreamsMap;
export {};
//# sourceMappingURL=generate-types.d.ts.map