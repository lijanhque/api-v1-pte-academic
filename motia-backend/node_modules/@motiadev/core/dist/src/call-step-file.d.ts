import type { Logger } from './logger';
import type { Motia } from './motia';
import type { Tracer } from './observability';
import type { InfrastructureConfig, Step } from './types';
type CallStepFileOptions = {
    step: Step;
    traceId: string;
    data?: unknown;
    contextInFirstArg?: boolean;
    logger: Logger;
    tracer: Tracer;
    infrastructure?: Partial<InfrastructureConfig>;
};
export declare const callStepFile: <TData>(options: CallStepFileOptions, motia: Motia) => Promise<TData | undefined>;
export {};
//# sourceMappingURL=call-step-file.d.ts.map