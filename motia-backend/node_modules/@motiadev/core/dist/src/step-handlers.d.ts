import type { EventAdapter } from './adapters/interfaces/event-adapter.interface';
import type { Motia } from './motia';
import type { EventConfig, Step } from './types';
export type MotiaEventManager = {
    createHandler: (step: Step<EventConfig>) => void;
    removeHandler: (step: Step<EventConfig>) => void;
};
export declare const createStepHandlers: (motia: Motia, eventAdapter: EventAdapter) => MotiaEventManager;
//# sourceMappingURL=step-handlers.d.ts.map