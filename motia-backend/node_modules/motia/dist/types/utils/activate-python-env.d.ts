interface VenvConfig {
    baseDir: string;
    isVerbose?: boolean;
    pythonVersion?: string;
}
export declare const getSitePackagesPath: ({ baseDir, pythonVersion }: VenvConfig) => string;
export declare const activatePythonVenv: ({ baseDir, isVerbose, pythonVersion }: VenvConfig) => void;
export {};
