import fs from 'fs';
import path from 'path';
export const checkIfFileExists = (dir, fileName) => {
    return fs.existsSync(path.join(dir, fileName));
};
export const checkIfDirectoryExists = (dir) => {
    try {
        return fs.statSync(dir).isDirectory();
    }
    catch {
        return false;
    }
};
