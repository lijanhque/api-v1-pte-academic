"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.stepBundleSizesValidator = void 0;
const colors_1 = __importDefault(require("colors"));
const path_1 = __importDefault(require("path"));
const constants_1 = require("./constants");
const stepBundleSizesValidator = (builder) => {
    const errors = [];
    for (const step of Object.values(builder.stepsConfig)) {
        const relativePath = path_1.default.relative(builder.projectDir, step.filePath);
        const stepUncompressedSize = builder.stepUncompressedSizes.get(step.filePath);
        if (stepUncompressedSize !== undefined) {
            const maxSize = constants_1.BUNDLE_SIZE_LIMITS.STEP_MAX_MB * constants_1.BYTES_TO_MB;
            if (stepUncompressedSize > maxSize) {
                const sizeMB = (stepUncompressedSize / constants_1.BYTES_TO_MB).toFixed(2);
                const compressedSize = builder.stepCompressedSizes.get(step.filePath);
                const compressedSizeMB = compressedSize ? (compressedSize / constants_1.BYTES_TO_MB).toFixed(2) : 'unknown';
                errors.push({
                    relativePath,
                    message: [
                        `Step bundle size exceeds ${constants_1.BUNDLE_SIZE_LIMITS.STEP_MAX_MB}MB limit (uncompressed).`,
                        `  ${colors_1.default.red('➜')} Uncompressed size: ${colors_1.default.magenta(sizeMB + 'MB')}`,
                        `  ${colors_1.default.red('➜')} Compressed size: ${colors_1.default.cyan(compressedSizeMB + 'MB')}`,
                        `  ${colors_1.default.red('➜')} Maximum allowed: ${colors_1.default.blue(constants_1.BUNDLE_SIZE_LIMITS.STEP_MAX_MB + 'MB')}`,
                    ].join('\n'),
                    step,
                });
            }
        }
    }
    return { errors, warnings: [] };
};
exports.stepBundleSizesValidator = stepBundleSizesValidator;
