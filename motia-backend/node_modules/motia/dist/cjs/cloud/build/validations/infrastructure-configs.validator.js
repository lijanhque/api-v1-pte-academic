"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.infrastructureConfigsValidator = void 0;
const core_1 = require("@motiadev/core");
const colors_1 = __importDefault(require("colors"));
const path_1 = __importDefault(require("path"));
const infrastructureConfigsValidator = (builder) => {
    const errors = [];
    const eventSteps = Object.values(builder.stepsConfig).filter((step) => step.config.type === 'event' && step.config.infrastructure);
    for (const step of eventSteps) {
        const config = step.config;
        const relativePath = path_1.default.relative(builder.projectDir, step.filePath);
        const validationResult = (0, core_1.validateInfrastructureConfig)(config.infrastructure);
        if (!validationResult.success && validationResult.errors) {
            for (const error of validationResult.errors) {
                errors.push({
                    relativePath,
                    message: [
                        `Infrastructure configuration error in step ${colors_1.default.magenta(step.config.name)}:`,
                        `  ${colors_1.default.red('➜')} ${error.path}: ${colors_1.default.red(error.message)}`,
                    ].join('\n'),
                    step,
                });
            }
        }
    }
    return { errors, warnings: [] };
};
exports.infrastructureConfigsValidator = infrastructureConfigsValidator;
