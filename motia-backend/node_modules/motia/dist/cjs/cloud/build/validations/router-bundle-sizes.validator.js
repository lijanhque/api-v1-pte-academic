"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.routerBundleSizesValidator = void 0;
const colors_1 = __importDefault(require("colors"));
const constants_1 = require("./constants");
const routerBundleSizesValidator = (builder) => {
    const errors = [];
    const maxRouterSize = constants_1.BUNDLE_SIZE_LIMITS.ROUTER_MAX_MB * constants_1.BYTES_TO_MB;
    for (const [routerType, uncompressedSize] of builder.routerUncompressedSizes.entries()) {
        if (uncompressedSize > maxRouterSize) {
            const uncompressedSizeMB = (uncompressedSize / constants_1.BYTES_TO_MB).toFixed(2);
            const compressedSize = builder.routerCompressedSizes.get(routerType);
            const compressedSizeMB = compressedSize ? (compressedSize / constants_1.BYTES_TO_MB).toFixed(2) : 'unknown';
            errors.push({
                relativePath: `${routerType} API router`,
                message: [
                    `${routerType.charAt(0).toUpperCase() + routerType.slice(1)} API router bundle size exceeds ${constants_1.BUNDLE_SIZE_LIMITS.ROUTER_MAX_MB}MB limit (uncompressed).`,
                    `  ${colors_1.default.red('➜')} Uncompressed size: ${colors_1.default.magenta(uncompressedSizeMB + 'MB')}`,
                    `  ${colors_1.default.red('➜')} Compressed size: ${colors_1.default.cyan(compressedSizeMB + 'MB')}`,
                    `  ${colors_1.default.red('➜')} Maximum allowed: ${colors_1.default.blue(constants_1.BUNDLE_SIZE_LIMITS.ROUTER_MAX_MB + 'MB')}`,
                ].join('\n'),
                step: Object.values(builder.stepsConfig)[0],
            });
        }
    }
    return { errors, warnings: [] };
};
exports.routerBundleSizesValidator = routerBundleSizesValidator;
