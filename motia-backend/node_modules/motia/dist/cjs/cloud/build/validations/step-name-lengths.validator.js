"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.stepNameLengthsValidator = void 0;
const colors_1 = __importDefault(require("colors"));
const path_1 = __importDefault(require("path"));
const constants_1 = require("./constants");
const stepNameLengthsValidator = (builder) => {
    const errors = [];
    for (const step of Object.values(builder.stepsConfig)) {
        if (step.config.name.length > constants_1.BUNDLE_SIZE_LIMITS.STEP_NAME_MAX_LENGTH) {
            const relativePath = path_1.default.relative(builder.projectDir, step.filePath);
            errors.push({
                relativePath,
                message: [
                    `Step name is too long. Maximum is ${constants_1.BUNDLE_SIZE_LIMITS.STEP_NAME_MAX_LENGTH} characters.`,
                    `  ${colors_1.default.red('➜')} ${colors_1.default.magenta(step.config.name)}`,
                ].join('\n'),
                step,
            });
        }
    }
    return { errors, warnings: [] };
};
exports.stepNameLengthsValidator = stepNameLengthsValidator;
