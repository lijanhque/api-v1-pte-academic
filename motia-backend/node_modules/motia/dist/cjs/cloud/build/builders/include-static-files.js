"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.includeStaticFiles = void 0;
const fs_1 = __importDefault(require("fs"));
const glob_1 = require("glob");
const path_1 = __importDefault(require("path"));
const includeStaticFiles = (steps, builder, archive) => {
    for (const step of steps) {
        if ('includeFiles' in step.config) {
            const staticFiles = step.config.includeFiles;
            if (!staticFiles || !Array.isArray(staticFiles) || staticFiles.length === 0) {
                continue;
            }
            staticFiles.forEach((file) => {
                const matches = (0, glob_1.globSync)(file, { cwd: path_1.default.dirname(step.filePath), absolute: true });
                matches.forEach((filePath) => {
                    const relativeFilePath = path_1.default.relative(builder.projectDir, filePath);
                    const content = fs_1.default.readFileSync(filePath);
                    archive.append(content, relativeFilePath);
                });
            });
        }
    }
};
exports.includeStaticFiles = includeStaticFiles;
